apply plugin: 'java'
apply plugin: 'application'

archivesBaseName = "LeapYears"
version = '1.0'
mainClassName = "LeapYears"

repositories {
	mavenCentral()
}

dependencies {
	testCompile group: 'junit', name: 'junit', version: '4.+'
}
